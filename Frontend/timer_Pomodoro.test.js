// Frontend/timer_Pomodoro.test.js
describe('Timer Pomodoro Tests', () => {
  test('Prueba básica 1', () => {
    expect(1 + 1).toBe(2);
  });

  test('Prueba básica 2', () => {
    expect('hello').toContain('hell');
  });

  test('Prueba básica 3', () => {
    expect([1, 2, 3]).toHaveLength(3);
  });
});